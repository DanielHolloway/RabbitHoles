description = "Entities (domain objects) and uses cases that implement the business logic"

dependencies {
    compile libs.spring_boot
    compile libs.string_utils

    testCompile libs.unit_tests
}